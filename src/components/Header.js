export function createHeader(){
    const header = document.querySelector('#header')
    header.className =
    "px-6  py-1 flex items-center justify-between" ;

    // player.innerHTMl = `
    
    // `
}

